#include <stdio.h>   // Includes the standard input-output library

int main()           // Main function where program execution starts
{
    printf("Name: Alex\n");      // Prints the name and moves to a new line
    printf("Age: 20\n");         // Prints the age and moves to a new line
    printf("Country: India\n");  // Prints the country and moves to a new line

    return 0;        // Ends the program and returns control to the OS
}
